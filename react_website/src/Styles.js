import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
      width: '30%',
      margin: '15px',
      height: '75%'
    },
    media: {
      height: '100%'
    },
});

export default useStyles